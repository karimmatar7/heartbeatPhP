{
    "temperature": [
      {"prompt": "icy, crystalline textures with frosted white highlights", "parameters": {"min": 0, "max": 35.0}},
      {"prompt": "cool, pale blue gradients with delicate textures", "parameters": {"min": 35.0, "max": 35.5}},
      {"prompt": "gentle, smooth cool tones blending blue and teal", "parameters": {"min": 35.5, "max": 36.0}},
      {"prompt": "neutral, soft gradients with a touch of orange warmth", "parameters": {"min": 36.0, "max": 36.5}},
      {"prompt": "warm, balanced hues with subtle fiery undertones", "parameters": {"min": 36.5, "max": 37.0}},
      {"prompt": "vivid, sharp tones with energetic red highlights", "parameters": {"min": 37.0, "max": 37.5}},
      {"prompt": "fiery, dynamic gradients with bold orange and yellow", "parameters": {"min": 37.5, "max": 38.0}},
      {"prompt": "blazing, molten reds with intense, glowing highlights", "parameters": {"min": 38.0, "max": 100.0}}
    ],
    "heartrate": [
      {"prompt": "soft, slow-moving blue waves with gentle transitions", "parameters": {"min": 0, "max": 40}},
      {"prompt": "calm, flowing blue shapes with subtle pulsations", "parameters": {"min": 40, "max": 50}},
      {"prompt": "gentle, rhythmic blue and green patterns", "parameters": {"min": 50, "max": 60}},
      {"prompt": "steady, smooth curves with red and orange accents", "parameters": {"min": 60, "max": 70}},
      {"prompt": "balanced, vibrant red hues with dynamic streaks", "parameters": {"min": 70, "max": 80}},
      {"prompt": "energetic, pulsing orange strokes with glowing edges", "parameters": {"min": 80, "max": 90}},
      {"prompt": "intense, swirling yellows with bursts of light", "parameters": {"min": 90, "max": 110}},
      {"prompt": "chaotic, rapid-fire patterns with explosive golden tones", "parameters": {"min": 110, "max": 1000}}
    ],
    "age": [
      {"prompt": "playful, whimsical patterns with colorful swirls", "parameters": {"min": 0, "max": 18}},
      {"prompt": "sleek, minimalist shapes with dynamic flow", "parameters": {"min": 18, "max": 20}},
      {"prompt": "fluid, organic curves with vibrant color transitions", "parameters": {"min": 20, "max": 22}},
      {"prompt": "layered geometric designs with a contemporary feel", "parameters": {"min": 22, "max": 24}},
      {"prompt": "modern, balanced patterns with subtle complexity", "parameters": {"min": 24, "max": 26}},
      {"prompt": "intricate, structured motifs with a refined aesthetic", "parameters": {"min": 26, "max": 28}},
      {"prompt": "sophisticated, mature designs with flowing symmetry", "parameters": {"min": 28, "max": 30}},
      {"prompt": "classic, layered textures with harmonious geometry", "parameters": {"min": 30, "max": 60}},
      {"prompt": "rich, tapestry-like designs evoking timelessness", "parameters": {"min": 60, "max": 1000}}
    ],
    "timestamp": [
      {"prompt": "soft, pastel tones with delicate movements", "parameters": {"min": 0, "max": 5}},
      {"prompt": "calm, flowing gradients with subtle contrasts", "parameters": {"min": 5, "max": 10}},
      {"prompt": "vibrant, dynamic lines with playful bursts of color", "parameters": {"min": 10, "max": 15}},
      {"prompt": "bold, glowing strokes with sharp edges", "parameters": {"min": 15, "max": 20}},
      {"prompt": "harmonious blends of light and shadow with smooth flow", "parameters": {"min": 20, "max": 25}},
      {"prompt": "dark, introspective tones with intricate overlays", "parameters": {"min": 25, "max": 30}},
      {"prompt": "energetic, chaotic splashes with radiant highlights", "parameters": {"min": 30, "max": 35}},
      {"prompt": "calming, gradient textures with soft luminescence", "parameters": {"min": 35, "max": 40}},
      {"prompt": "sharp, contrasting patterns with glowing outlines", "parameters": {"min": 40, "max": 45}},
      {"prompt": "warm, golden streaks with a hint of radiance", "parameters": {"min": 45, "max": 50}},
      {"prompt": "cool, misty blues with layered transparency", "parameters": {"min": 50, "max": 55}},
      {"prompt": "vivid, kaleidoscopic forms with shifting symmetry", "parameters": {"min": 55, "max": 60}}
    ]
  }